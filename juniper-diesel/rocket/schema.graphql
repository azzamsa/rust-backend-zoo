type Health {
  status: String!
}

type Meta {
  build: String!
  version: String!
}

type Query {
  health: Health!
  meta: Meta!
  users: [User!]!
  user(id: Int!): User!
}

type User {
  id: Int!
  name: String!
  fullName: String
}

